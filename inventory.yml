deploy_servers:
  hosts:
    server1:
      ansible_host: "192.168.56.102"
      ansible_user: "gx"
      ansible_password: "{{ vault_deploy_server1_password }}"
      ansible_become: yes
      ansible_become_password: "{{ vault_deploy_server1_password }}"
